spring:
  datasource:
    url: jdbc:mysql://localhost/ray
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: create-drop
  redis:
    host: localhost
    port: 6379

logging:
  file: Flamingo.log
